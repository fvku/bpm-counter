# BPM Counter

シングルファイルのDJ向けBPMカウンターWebアプリ。

## ファイル

- `index.html` — 唯一のファイル。HTML/CSS/JSをすべて内包。

## 機能

- **タップ入力** — ボタンまたはスペースキーでタップしてBPM計算
- **±1ボタン** — TAPボタンの左右に配置。現在のBPMを手動で微調整
- **マイク入力** — Web Audio APIで音量スパイクを検出してBPM推定。感度スライダーあり
- **ビートインジケーター** — 8つの●ドット＋8つの+記号が交互に点灯（ドラム教育の「1 + 2 + 3 + 4 +」表記）。タップのたびに1拍目からリスタート
- **BPM履歴** — 最大20件、タップ/マイクのソースと時刻付きで表示
- **リセット** — タップ履歴・BPM・ビートをクリア

## デザイン方針

- DJブース使用を想定したミニマル設計。余計な情報は出さない
- 背景 `#0a0a0a`、テキスト白系、アクセントなし
- テンポ名（Allegro等）は**表示しない**（過去に削除済み）

## 公開予定

GitHub Pages または Netlify でホスティングしてスマホからも使えるようにする予定。マイク使用のためHTTPS必須（両サービスとも対応済み）。
